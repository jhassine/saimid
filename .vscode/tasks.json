{

    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "problemMatcher": [
                "$python"
            ],
            "label": "devserver: Run the development server",
            "command": ["python", "manage.py", "runserver", "0.0.0.0:8000"],
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}
